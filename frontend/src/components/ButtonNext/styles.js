import styled from "styled-components";
import icon from "../../icons/icon-proximo.svg";

export const Icon = styled.h1`
  align-self: flex-end;
  background: url(${icon}) no-repeat center right;
  width: 16px;
  height: 16px;
  margin: 30px 10px;
`;
